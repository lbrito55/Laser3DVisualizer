import { Scene, FreeCamera as BabylonFreeCamera, Camera, Vector3 } from "babylonjs";
import SceneComponent, { SceneComponentProps } from "./SceneComponent";
import { CameraPropsHandler, CameraProps, CameraComponent } from "./Camera";
export declare type FreeCameraProps = {
    position?: Vector3;
    x?: number;
    y?: number;
    z?: number;
    target?: Vector3;
    setActiveOnSceneIfNoneActive?: boolean;
} & CameraProps & SceneComponentProps<BabylonFreeCamera>;
export default class FreeCamera extends SceneComponent<BabylonFreeCamera, Camera, FreeCameraProps> implements CameraComponent {
    private _camera?;
    componentsCreated(): void;
    readonly camera: BabylonFreeCamera;
    readonly noPreventDefault: boolean;
    create(scene: Scene): BabylonFreeCamera;
    readonly propsHandlers: CameraPropsHandler[];
    componentWillReceiveProps(): void;
}
