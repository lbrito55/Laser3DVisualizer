import { Component } from "react";
import { Behavior, ComponentContainerProps } from "./SceneComponent";
import { Scene, Vector3, AbstractMesh } from "babylonjs";
export declare type RotateMeshBehaviorProps = {
    axis: string | Vector3;
    radians: number;
} & ComponentContainerProps;
export default class RotateMeshBehaviour extends Component<RotateMeshBehaviorProps, {}> implements Behavior<AbstractMesh> {
    behaviourAdded: boolean;
    options: any;
    constructor(props: RotateMeshBehaviorProps);
    apply(target: AbstractMesh, scene: Scene): void;
    render(): null;
}
