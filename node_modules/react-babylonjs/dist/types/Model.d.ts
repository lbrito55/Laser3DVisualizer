import { Scene, IParticleSystem, Skeleton, AnimationGroup, AbstractMesh, Nullable, BoundingInfo } from "babylonjs";
import "babylonjs-loaders";
import SceneComponent from "./SceneComponent";
import ModelPropsHandler, { ModelProps } from "./ModelProps";
export declare enum LoaderStatus {
    Loading = 0,
    Loaded = 1,
    Error = 2
}
export declare class LoadedModel {
    status?: LoaderStatus;
    rootMesh?: AbstractMesh;
    errorMessage?: string;
    loaderName?: string;
    meshes?: AbstractMesh[];
    particleSystems?: IParticleSystem[];
    skeletons?: Skeleton[];
    animationGroups?: AnimationGroup[];
    scaleToDimension?: number;
    readonly boundingInfo: Nullable<BoundingInfo>;
    /**
     * Clean up all resources.
     */
    dispose(): void;
}
export default class Model extends SceneComponent<LoadedModel, LoadedModel, ModelProps> {
    protected options: any;
    private loadedModel?;
    create(scene: Scene): LoadedModel;
    componentsCreated(): void;
    componentWillUnmount(): void;
    readonly propsHandlers: ModelPropsHandler[];
}
