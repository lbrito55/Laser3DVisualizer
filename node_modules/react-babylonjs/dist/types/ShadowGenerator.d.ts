import { Scene, ShadowGenerator as BabylonShadowGenerator } from "babylonjs";
import SceneComponent, { SceneComponentProps } from "./SceneComponent";
export declare type ShadowGeneratorProps = {
    mapSize: number;
    useBlurExponentialShadowMap: boolean;
    blurKernel: number;
    shadowCasters: string[];
} & SceneComponentProps<BabylonShadowGenerator>;
export default class ShadowGenerator extends SceneComponent<BabylonShadowGenerator, BabylonShadowGenerator, ShadowGeneratorProps> {
    private shadowGenerator?;
    create(scene: Scene): BabylonShadowGenerator;
    componentsCreated(): void;
    readonly propsHandlers: never[];
}
