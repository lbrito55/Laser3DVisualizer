import { Component } from "react";
import { Material, ComponentContainerProps } from "./SceneComponent";
import { Scene, Material as BabylonMaterial, AbstractMesh, Color3 } from "babylonjs";
export declare type StandardMaterialProps = {
    ambientColor?: Color3;
    diffuseColor?: Color3;
    emmissiveColor?: Color3;
    specularColor?: Color3;
} & ComponentContainerProps;
export default class StandardMaterial extends Component<StandardMaterialProps, {}> implements Material<AbstractMesh> {
    material?: BabylonMaterial;
    materialSet: boolean;
    options: any;
    constructor(props: StandardMaterialProps);
    apply(target: AbstractMesh, scene: Scene): void;
    render(): null;
}
